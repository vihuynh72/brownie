# Brownie

Brownie is a personal project for building and reviewing source-backed meeting
minutes.

## Run

There is no application to run yet. Check the pinned local toolchain with:

```sh
./scripts/verify-toolchain.sh
```
