package io.github.vihuynh72.brownie.core.template;

/** DRAFT until the first version is activated; ACTIVE from then on, even while a newer draft is being edited alongside it. */
public enum TemplateStatus {
    DRAFT,
    ACTIVE
}
