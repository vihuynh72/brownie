package io.github.vihuynh72.brownie.core.workspace;

/** Only MANAGE_WORKSPACE exists today; finer-grained capabilities arrive once team roles do. */
public enum WorkspaceCapability {
    MANAGE_WORKSPACE
}
