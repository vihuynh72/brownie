package io.github.vihuynh72.brownie.core.revision;

/** OPEN until acted on exactly once, the same shape {@code QuestionStatus} already establishes for a comparable one-shot resource. */
public enum PatchProposalStatus {
    PROPOSED,
    ACCEPTED
}
