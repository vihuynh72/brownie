package io.github.vihuynh72.brownie.core.template;

/** A DRAFT version's field definitions can still be replaced. ACTIVATED is permanent -- see {@link TemplateVersion}. */
public enum TemplateVersionStatus {
    DRAFT,
    ACTIVATED
}
